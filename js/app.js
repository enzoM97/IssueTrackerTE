$(function() {
  const g_todos = [];
  const g_inProgress = [];
  const g_testing = [];
  const g_completed = [];

  $('#btnAddTodos').click(function (e) { 
    e.preventDefault();
    console.log('clicked');
  });

});
